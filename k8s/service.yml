apiVersion: v1
kind: Service
metadata:
  name: weatherapp
spec:
  selector:
    app: weatherapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30008
  type: NodePort
